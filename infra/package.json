{
  "name": "aws-bedrock-deepseek",
  "version": "1.0.0",
  "description": "Pulumi project for AWS Bedrock with Deepseek R1 7B model",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/awsx": "^2.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "@pulumi/docker",
      "@pulumi/docker-build",
      "protobufjs"
    ]
  }
}